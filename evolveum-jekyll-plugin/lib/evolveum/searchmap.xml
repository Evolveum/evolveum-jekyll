<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://schema.evolveum.com/www/searchmap">
    {% for page in site.pages %}
        {% if page.visibility != "system" and page.visibility != "auxiliary" and page.visibility != "hidden" %}
    <url>
        <loc>{{ page.url | absolute_url | xml_escape }}</loc>
        {% if page.lastModificationDate %}<lastmod>{{ page.lastModificationDate | date_to_xmlschema }}</lastmod>{% endif %}
        {% if page.title %}<title>{{ page.title | xml_escape }}</title>{% endif %}
        {% if page.keywords %}<keywords>{{ page.keywords | xml_escape }}</keywords>{% endif %}
        {% if page.description %}<description>{{ page.description | xml_escape }}</description>{% endif %}
        {% if page.author %}<author>{{ page.author | xml_escape }}</author>{% endif %}
    </url>
        {% endif %}
    {% endfor %}
</urlset>
