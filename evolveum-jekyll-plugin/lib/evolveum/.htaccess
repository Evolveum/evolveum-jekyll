{% for redirect in page.redirects %}
RewriteRule   "{{ redirect.pattern }}" "{{ redirect.substitution }}" [R]
{% endfor %}
RewriteRule   "^midpoint/reference(?!/latest)(?!/v[0-9])(/|$)(.*)" "/midpoint/reference/latest/$4" [R]
