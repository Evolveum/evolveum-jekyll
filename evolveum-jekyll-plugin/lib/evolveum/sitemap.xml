<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:page="http://schema.evolveum.com/www/sitemap">
    {% for page in site.pages %}
        {% if page.visibility != "system" and page.visibility != "auxiliary" and page.visibility != "hidden" %}
    <url>
        <loc>{{ page.url | absolute_url | xml_escape }}</loc>
        {% if page.lastModificationDate %}<lastmod>{{ page.lastModificationDate | date_to_xmlschema }}</lastmod>{% endif %}
        {% if page.title %}<page:title>{{ page.title | xml_escape }}</page:title>{% endif %}
        {% if page.keywords %}<page:keywords>{{ page.keywords | xml_escape }}</page:keywords>{% endif %}
        {% if page.description %}<page:description>{{ page.description | xml_escape }}</page:description>{% endif %}
        {% if page.author %}<page:author>{{ page.author | xml_escape }}</page:author>{% endif %}
    </url>
        {% endif %}
    {% endfor %}
</urlset>
